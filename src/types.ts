export type SkillType = {
  name: string;
  percentage: number;
  level: string;
  color: string;
  src: string;
};

export type QualificationType = {
  name: string;
  language: string;
  date: string;
  link: string;
};
